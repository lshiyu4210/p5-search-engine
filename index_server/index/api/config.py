"""Global variables."""
from collections import defaultdict
# Define global variables here
INDICES = defaultdict(dict)
PAGERANK = {}
STOPWORDS = []

print("Inverted Indexes, PageRank, and Stopwords created.")
